# MongoDB connection string (get free from https://mongodb.com/atlas)
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/moneypp?retryWrites=true&w=majority

# Server port (optional, default 8080)
PORT=8080

# Server host (optional, default 0.0.0.0)
HOST=0.0.0.0
